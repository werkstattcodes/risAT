% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ris_search_justiz.R
\name{ris_search_justiz}
\alias{ris_search_justiz}
\title{Search Justiz Decisions in RIS}
\usage{
ris_search_justiz(
  query = NULL,
  business_number = NULL,
  norm = NULL,
  decision_date_from = NULL,
  decision_date_to = NULL,
  decision_type = NULL,
  index_term = NULL,
  legal_area = NULL,
  specialist_area = NULL,
  court = NULL,
  legal_principle_number = NULL,
  legal_assessment = NULL,
  ruling = NULL,
  citation = NULL,
  changed_since_period = NULL,
  in_ris_since = NULL,
  search_decision_text = TRUE,
  search_legal_principles = TRUE,
  echo = FALSE,
  max_pages = Inf,
  base_url = ris_base_url()
)
}
\arguments{
\item{query}{Optional full-text query (\code{Suchworte}).}

\item{business_number}{Optional business number (\code{Geschaeftszahl}).}

\item{norm}{Optional legal norm query (\code{Norm}).}

\item{decision_date_from}{Optional lower date bound (\code{YYYY-MM-DD},
\code{EntscheidungsdatumVon}).}

\item{decision_date_to}{Optional upper date bound (\code{YYYY-MM-DD},
\code{EntscheidungsdatumBis}).}

\item{decision_type}{Optional decision type (\code{Entscheidungsart}).
Justiz accepts (pass the string exactly as shown):
\code{"Ordentliche Erledigung (Sachentscheidung)"},
\code{"Zurückweisung mangels erheblicher Rechtsfrage"},
\code{"Zurückweisung aus anderen Gründen"},
\code{"Verstärkter Senat"}.}

\item{index_term}{Optional index term (\code{Index}).}

\item{legal_area}{Optional legal area filter (\code{Rechtsgebiet}), e.g.
\code{"Zivilrecht"} or \code{"Strafrecht"}.}

\item{specialist_area}{Optional specialist area (\code{Fachgebiet}), e.g.
\code{"Arbeitsrecht"} or \code{"Mietrecht"}.}

\item{court}{Optional court name (\code{Gericht}), e.g. \code{"OGH"}, \code{"OLG Wien"}.}

\item{legal_principle_number}{Optional legal principle number
(\code{Rechtssatznummer}), e.g. \code{"0000001"}.}

\item{legal_assessment}{Optional legal assessment text
(\code{RechtlicheBeurteilung}).}

\item{ruling}{Optional ruling text (\code{Spruch}).}

\item{citation}{Optional citation reference (\code{Fundstelle}),
e.g. \code{"SZ 75/123"}.}

\item{changed_since_period}{Optional time window for changes
(\code{AenderungenSeitPeriode}). Same interval keywords as \code{in_ris_since}.}

\item{in_ris_since}{Optional RIS recency filter (\code{ImRisSeit}).
Accepts API values (\code{"Undefined"}, \code{"EinerWoche"}, \code{"ZweiWochen"},
\code{"EinemMonat"}, \code{"DreiMonaten"}, \code{"SechsMonaten"}, \code{"EinemJahr"}) and
English aliases (\code{"one_week"}, \code{"two_weeks"}, \code{"one_month"},
\code{"three_months"}, \code{"six_months"}, \code{"one_year"}).}

\item{search_decision_text}{Optional flag for decision text search
(\code{SucheInEntscheidungstexten}).}

\item{search_legal_principles}{Optional flag for legal principles search
(\code{SucheInRechtssaetzen}).}

\item{echo}{Logical. If \code{TRUE}, prints the equivalent RIS website URLs
(\verb{https://www.ris.bka.gv.at/<Applikation>/} and the corresponding
\code{Ergebnis.wxe} query URL) and the number of returned rows, so users can
double-check the result set in the browser.}

\item{max_pages}{Maximum number of result pages to fetch (100 documents
per page). Defaults to \code{Inf} (fetch all pages in scope). When the limit
truncates the result set, a message says how to get the rest. Use this
to keep broad exploratory queries from firing hundreds of requests at
the public RIS API.}

\item{base_url}{API base URL. Defaults to \code{\link[=ris_base_url]{ris_base_url()}}, which can be
overridden for a session via \code{options(risAT.base_url = ...)}.}
}
\value{
A tidy tibble with parsed search results.
Includes list-column \code{content_urls}.
}
\description{
Convenience wrapper around \code{\link[=ris_search_case_law]{ris_search_case_law()}} with
\code{application = "Justiz"}.
}
\details{
Covers decisions from the Oberster Gerichtshof (OGH), Oberlandesgerichte
(OLG), Landesgerichte (LG), Bezirksgerichte (BG), Oberster Patent- und
Markensenat (OPMS, until 2013), and selected foreign decisions (AUSL).
}
\examples{
\dontrun{
# Keyword search across decision texts and Rechtssaetze (default).
# The query field supports full-text operators: space/"und" = AND,
# "oder" = OR, "nicht" = NOT, * = wildcard, 'phrase' for exact phrase.
ris_search_justiz(query = "Schadenersatz")

# Filter to OGH decisions only
ris_search_justiz(query = "Schadenersatz", court = "OGH")

# Filter by legal area and date range
ris_search_justiz(
  legal_area = "Zivilrecht",
  decision_date_from = "2022-01-01",
  decision_date_to = "2023-12-31"
)

# Search by legal principle number (Rechtssatznummer)
ris_search_justiz(legal_principle_number = "0000001")

# Search by citation (Fundstelle)
ris_search_justiz(citation = "SZ 75/123")

# Search by norm and restrict to OGH decisions in a specialist area
ris_search_justiz(
  norm = "ABGB §1295",
  specialist_area = "Schadenersatzrecht",
  court = "OGH"
)

# Look up a specific OGH case by business number
# OGH business number format: "N Ob NNN/YY" or "N Ob N/YYg" etc.
ris_search_justiz(business_number = "1Ob1/23g", echo = TRUE)
}
}
