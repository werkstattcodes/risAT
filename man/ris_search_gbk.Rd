% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ris_search_gbk.R
\name{ris_search_gbk}
\alias{ris_search_gbk}
\title{Search Equal Treatment Commission Decisions in RIS}
\usage{
ris_search_gbk(
  query = NULL,
  business_number = NULL,
  norm = NULL,
  decision_date_from = NULL,
  decision_date_to = NULL,
  decision_type = NULL,
  commission = NULL,
  senate = NULL,
  discrimination_ground = NULL,
  in_ris_since = NULL,
  echo = FALSE,
  base_url = "https://data.bka.gv.at/ris/api/v2.6"
)
}
\arguments{
\item{query}{Optional full-text query (\code{Suchworte}).}

\item{business_number}{Optional business number (\code{Geschaeftszahl}).}

\item{norm}{Optional legal norm query (\code{Norm}).}

\item{decision_date_from}{Optional lower date bound (\code{YYYY-MM-DD},
\code{EntscheidungsdatumVon}).}

\item{decision_date_to}{Optional upper date bound (\code{YYYY-MM-DD},
\code{EntscheidungsdatumBis}).}

\item{decision_type}{Optional decision type (\code{Entscheidungsart}).
Gbk accepts: \code{"Undefined"}, \code{"Einzelfallpruefungsergebnis"},
\code{"Gutachten"}.}

\item{commission}{Optional commission filter (\code{Kommission}), e.g.
\code{"Bundes-Gleichbehandlungskommission"} or
\code{"Gleichbehandlungskommission"}.}

\item{senate}{Optional senate filter (\code{Senat}), e.g. \code{"Senat I"},
\code{"Senat II"}, \code{"Senat III"}.}

\item{discrimination_ground}{Optional discrimination ground
(\code{Diskriminierungsgrund}), e.g. \code{"Geschlecht"}, \code{"Ethnische Zugehörigkeit"},
\code{"Religion"}, \code{"Weltanschauung"}, \code{"Alter"}, \code{"Sexuelle Orientierung"}.}

\item{in_ris_since}{Optional RIS recency filter (\code{ImRisSeit}).
Accepts API values (\code{"Undefined"}, \code{"EinerWoche"}, \code{"ZweiWochen"},
\code{"EinemMonat"}, \code{"DreiMonaten"}, \code{"SechsMonaten"}, \code{"EinemJahr"}) and
English aliases (\code{"one_week"}, \code{"two_weeks"}, \code{"one_month"},
\code{"three_months"}, \code{"six_months"}, \code{"one_year"}).}

\item{echo}{Logical. If \code{TRUE}, prints the equivalent RIS website URLs
(\verb{https://www.ris.bka.gv.at/<Applikation>/} and the corresponding
\code{Ergebnis.wxe} query URL) and the number of returned rows, so users can
double-check the result set in the browser.}

\item{base_url}{API base URL.}
}
\value{
A tidy tibble with parsed search results.
Includes list-columns \code{content_urls} and \code{app_metadata}.
}
\description{
Convenience wrapper around \code{\link[=ris_search_case_law]{ris_search_case_law()}} with
\code{application = "Gbk"}.
}
\details{
Covers anonymised decisions of the Bundes-Gleichbehandlungskommission
(Senate I and II) and the Gleichbehandlungskommission für die
Privatwirtschaft (Senate I, II, and III) since 1 January 2014.

Note: the Gbk application does not support \code{search_decision_text} or
\code{search_legal_principles} filtering.  Those parameters are not available
for this wrapper.
}
\examples{
\dontrun{
# Search all equal treatment commission decisions
ris_search_gbk(query = "Diskriminierung")

# Filter to opinions (Gutachten) on gender discrimination
ris_search_gbk(
  decision_type = "Gutachten",
  discrimination_ground = "Geschlecht"
)

# Filter to individual case review results in the private sector commission
ris_search_gbk(
  decision_type = "Einzelfallpruefungsergebnis",
  commission = "Gleichbehandlungskommission"
)

# Search by norm and senate
ris_search_gbk(
  norm = "GlBG §17",
  senate = "Senat I"
)
}
}
