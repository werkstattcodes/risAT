% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ris_search_pvak.R
\name{ris_search_pvak}
\alias{ris_search_pvak}
\title{Search Staff Representation Oversight Decisions in RIS}
\usage{
ris_search_pvak(
  query = NULL,
  business_number = NULL,
  norm = NULL,
  decision_date_from = NULL,
  decision_date_to = NULL,
  decision_type = NULL,
  deciding_authority = NULL,
  in_ris_since = NULL,
  search_decision_text = TRUE,
  search_legal_principles = TRUE,
  echo = FALSE,
  base_url = "https://data.bka.gv.at/ris/api/v2.6"
)
}
\arguments{
\item{query}{Optional full-text query (\code{Suchworte}).}

\item{business_number}{Optional business number (\code{Geschaeftszahl}).}

\item{norm}{Optional legal norm query (\code{Norm}).}

\item{decision_date_from}{Optional lower date bound (\code{YYYY-MM-DD},
\code{EntscheidungsdatumVon}).}

\item{decision_date_to}{Optional upper date bound (\code{YYYY-MM-DD},
\code{EntscheidungsdatumBis}).}

\item{decision_type}{Optional decision type (\code{Entscheidungsart}).
Free-text; the API validates server-side.}

\item{deciding_authority}{Optional filter for the deciding body
(\code{EntscheidendeBehoerde}), e.g. \code{"Personalvertretungsaufsichtsbehörde"}.}

\item{in_ris_since}{Optional RIS recency filter (\code{ImRisSeit}).
Accepts API values (\code{"Undefined"}, \code{"EinerWoche"}, \code{"ZweiWochen"},
\code{"EinemMonat"}, \code{"DreiMonaten"}, \code{"SechsMonaten"}, \code{"EinemJahr"}) and
English aliases (\code{"one_week"}, \code{"two_weeks"}, \code{"one_month"},
\code{"three_months"}, \code{"six_months"}, \code{"one_year"}).}

\item{search_decision_text}{Optional flag for decision text search
(\code{SucheInEntscheidungstexten}).}

\item{search_legal_principles}{Optional flag for legal principles search
(\code{SucheInRechtssaetzen}).}

\item{echo}{Logical. If \code{TRUE}, prints the equivalent RIS website URLs
(\verb{https://www.ris.bka.gv.at/<Applikation>/} and the corresponding
\code{Ergebnis.wxe} query URL) and the number of returned rows, so users can
double-check the result set in the browser.}

\item{base_url}{API base URL.}
}
\value{
A tidy tibble with parsed search results.
Includes list-columns \code{content_urls} and \code{app_metadata}.
}
\description{
Convenience wrapper around \code{\link[=ris_search_case_law]{ris_search_case_law()}} with
\code{application = "Pvak"}.
}
\details{
Covers decisions of Austria's staff representation oversight bodies: the
Personalvertretungs-Aufsichtskommission (PVAK, 1999–2013) and the
Personalvertretungsaufsichtsbehörde (PVAB, since 2014).
}
\examples{
\dontrun{
# Search all staff representation oversight decisions
ris_search_pvak(query = "Wahlrecht")

# Filter to PVAB decisions (since 2014)
ris_search_pvak(
  deciding_authority = "Personalvertretungsaufsichtsbehörde",
  decision_date_from = "2014-01-01"
)

# Search by norm
ris_search_pvak(norm = "BPVG §22")
}
}
