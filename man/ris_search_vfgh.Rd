% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ris_search_vfgh.R
\name{ris_search_vfgh}
\alias{ris_search_vfgh}
\title{Search VfGH Decisions in RIS}
\usage{
ris_search_vfgh(
  query = NULL,
  business_number = NULL,
  norm = NULL,
  decision_date_from = NULL,
  decision_date_to = NULL,
  decision_type = NULL,
  index_term = NULL,
  collection_number = NULL,
  in_ris_since = NULL,
  search_decision_text = FALSE,
  search_legal_principles = TRUE,
  echo = FALSE,
  base_url = ris_base_url()
)
}
\arguments{
\item{query}{Optional full-text query (\code{Suchworte}). Supports the RIS
full-text operators (space/\code{und} = AND, \code{OR}/\code{ODER} = OR, \code{nicht} = NOT,
\code{*} = wildcard, \code{'phrase'} for exact phrase).}

\item{business_number}{Optional business number (\code{Geschaeftszahl}).}

\item{norm}{Optional legal norm query (\code{Norm}). Multiple norms can be
combined with \code{OR}/\code{ODER} (wrap each norm in single quotes, e.g.
\code{"'AsylG 2005 §3' ODER 'BFA-VG §21 Abs7'"}).}

\item{decision_date_from}{Optional lower date bound (\code{YYYY-MM-DD},
\code{EntscheidungsdatumVon}).}

\item{decision_date_to}{Optional upper date bound (\code{YYYY-MM-DD},
\code{EntscheidungsdatumBis}).}

\item{decision_type}{Optional decision type (\code{Entscheidungsart}).
VfGH accepts: \code{"Undefined"}, \code{"Beschluss"}, \code{"Erkenntnis"},
\code{"Vergleich"}, \code{"KeineAngabe"} (English aliases: \code{"order"}, \code{"judgment"},
\code{"settlement"}, \code{"not_specified"}).}

\item{index_term}{Optional index term (\code{Index}).}

\item{collection_number}{Optional collection number (\code{Sammlungsnummer}).}

\item{in_ris_since}{Optional RIS recency filter (\code{ImRisSeit}).
Accepts API values (\code{"Undefined"}, \code{"EinerWoche"}, \code{"ZweiWochen"},
\code{"EinemMonat"}, \code{"DreiMonaten"}, \code{"SechsMonaten"}, \code{"EinemJahr"}) and
English aliases (\code{"one_week"}, \code{"two_weeks"}, \code{"one_month"},
\code{"three_months"}, \code{"six_months"}, \code{"one_year"}).}

\item{search_decision_text}{Optional flag for decision text search
(\code{SucheInEntscheidungstexten}).}

\item{search_legal_principles}{Optional flag for legal principles search
(\code{SucheInRechtssaetzen}). When both flags are omitted, both document
types are searched. When only one flag is given, the other defaults to
its complement, so a single flag selects exactly one document type
(e.g. \code{search_decision_text = FALSE} searches legal principles only).
Setting both to \code{FALSE} is an error.}

\item{echo}{Logical. If \code{TRUE}, prints three progress messages: the
equivalent RIS website search URL (the \code{Ergebnis.wxe} query on
\verb{https://www.ris.bka.gv.at}) before any request is sent; the total hit
and page count as soon as the first page's response arrives; and the
final number of returned rows once all pages have been fetched. This
lets the result set be double-checked in the browser and gives an early
sense of scope for broad queries without waiting for every page.}

\item{base_url}{API base URL. Defaults to \code{\link[=ris_base_url]{ris_base_url()}}, which can be
overridden for a session via \code{options(risAT.base_url = ...)}.}
}
\value{
A tidy tibble with parsed search results.
Includes list-column \code{content_urls}.
}
\description{
Convenience wrapper around \code{\link[=ris_search_case_law]{ris_search_case_law()}} with
\code{application = "Vfgh"}.
}
\details{
Defaults are aligned with the VfGH RIS handbook:
\code{search_legal_principles = TRUE} and \code{search_decision_text = FALSE}
(default search in Rechtssaetze).
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Search Rechtssaetze (default per VfGH handbook) for a constitutional keyword.
# The query field supports full-text operators: space/"und" = AND,
# "OR"/"ODER" = OR, "nicht" = NOT, * = wildcard, 'phrase' for exact phrase.
ris_search_vfgh(query = "Meinungsfreiheit")

# Wildcard and phrase search examples
ris_search_vfgh(query = "Grundrecht*")
ris_search_vfgh(query = "'wahlwerbenden Parteien'")

# Also search full decision texts (overrides the VfGH default of RS only)
ris_search_vfgh(
  query = "Eigentumsrecht",
  search_decision_text = TRUE,
  search_legal_principles = TRUE
)

# Judgments on a specific norm. Results are always sorted by decision date
# descending (most recent first).
# Norm notation: include the year where it is part of the official
# abbreviation (e.g. "StGG Art2", "EStG 1988 §29 Z1", "AsylG 2005 §5").
# For multiple norms wrap each in single quotes: 'StGG Art2' ODER 'B-VG Art7'
ris_search_vfgh(
  norm = "B-VG Art7",
  decision_type = "judgment"
)

# Search by Sammlungsnummer (collection number from the official VfGH
# series VfSlg). Enter digits only, without dot, space, or slash.
ris_search_vfgh(collection_number = "18743")

# Decisions added to RIS within the last six months
ris_search_vfgh(in_ris_since = "six_months")

# Search by Index (numeric classification of Austrian law).
# Federal law index values start with a number (e.g. "32/02" for Steuerrecht,
# "07/01" for Verfassungsrecht), state law index values start with "L"
# (e.g. "L6500"). The full index list is linked from the VfGH search page.
ris_search_vfgh(index_term = "07/01")

# Look up a specific case by business number and echo the equivalent
# browser URL on www.ris.bka.gv.at.
# VfGH decisions are available from 1980 in full text; selected decisions
# from 1919-1933 and 1946-1979 are available as PDF scans.
# Business number formats (four-digit year since 08.04.2013):
#   G NNNN/YYYY  (constitutional review of laws, Gesetzesprüfung)
#   V NNN/YYYY   (review of ordinances, Verordnungsprüfung)
#   U NNN/YYYY   (individual complaint, Art144 B-VG)
#   E NNN/YYYY   (complaint under EU Charter of Fundamental Rights)
#   B NNN/YY     (old-style individual complaint, pre-2013)
ris_search_vfgh(
  business_number = "G 97/2021",
  echo = TRUE
)
\dontshow{\}) # examplesIf}
}
