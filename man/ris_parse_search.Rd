% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ris_parse_search.R
\name{ris_parse_search}
\alias{ris_parse_search}
\title{Parse RIS Search Responses}
\usage{
ris_parse_search(x, requested_page = NULL, requested_per_page = NULL)
}
\arguments{
\item{x}{An \code{httr2_response} or a decoded list.}

\item{requested_page}{Optional requested page number for metadata fallback.}

\item{requested_per_page}{Optional requested page size for metadata fallback.}
}
\value{
A tibble parsed from one RIS response payload.
Includes list-columns \code{content_urls} and \code{app_metadata}.
}
\description{
Parse a RIS OGD REST API search response into a tidy tibble.
The parser works with either an \code{httr2_response} object or an already
decoded response list.
}
\examples{
payload <- list(
  OgdSearchResult = list(
    status = "ok",
    OgdDocumentResults = list(
      OgdDocumentReference = list(
        list(
          Data = list(
            Metadaten = list(
              Technisch = list(ID = "DOC-1", Applikation = "Vwgh"),
              Allgemein = list(DokumentUrl = "https://example.org/meta/1")
            ),
            Dokumentliste = list(
              ContentReference = list(
                list(
                  Urls = list(
                    ContentUrl = list(
                      list(DataType = "Html", Url = "https://example.org/doc/1.html")
                    )
                  )
                )
              )
            )
          )
        )
      )
    )
  )
)
ris_parse_search(payload)
}
