% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ris_req_federal.R
\name{ris_req_federal}
\alias{ris_req_federal}
\title{Build a RIS Bundesrecht API Request}
\usage{
ris_req_federal(
  query = NULL,
  title = NULL,
  index = NULL,
  type = NULL,
  law_number = NULL,
  promulgation_organ = NULL,
  promulgation_number = NULL,
  signature_date = NULL,
  version_date = NULL,
  effective_from = NULL,
  effective_to = NULL,
  expiry_from = NULL,
  expiry_to = NULL,
  section_from = NULL,
  section_to = NULL,
  section_type = NULL,
  in_ris_since = NULL,
  sort_by = NULL,
  sort_direction = NULL,
  base_url = ris_base_url()
)
}
\arguments{
\item{query}{Optional full-text query (\code{Suchworte}). Supports the RIS
full-text operators (space/\code{und} = AND, \code{oder} = OR, \code{nicht} = NOT,
\code{*} = wildcard, \code{'phrase'} for exact phrase).}

\item{title}{Optional title or abbreviation of the legal norm (\code{Titel}).}

\item{index}{Optional index reference from the systematic directory of
federal law (\code{Index}, e.g. \code{"20/01"}).}

\item{type}{Optional norm type (\code{Typ}).}

\item{law_number}{Optional law number (\code{Gesetzesnummer}), an exact-match
identifier for the consolidated norm.}

\item{promulgation_organ}{Optional promulgation organ (\code{Kundmachungsorgan}),
e.g. \code{"BGBl. I Nr."}, \code{"BGBl. II Nr."}, \code{"RGBl. Nr."}.}

\item{promulgation_number}{Optional promulgation number
(\code{Kundmachungsorgannummer}, e.g. \code{"25/2012"}).}

\item{signature_date}{Optional signature date (\code{Unterzeichnungsdatum},
\code{YYYY-MM-DD}).}

\item{version_date}{Optional point-in-time version date
(\code{Fassung.FassungVom}, \code{YYYY-MM-DD}): retrieve the consolidated text as it
stood on this date. Cannot be combined with the \verb{effective_*} / \verb{expiry_*}
range arguments.}

\item{effective_from, effective_to}{Optional entry-into-force date range
(\code{Fassung.VonInkrafttretensdatum} / \code{Fassung.BisInkrafttretensdatum},
\code{YYYY-MM-DD}).}

\item{expiry_from, expiry_to}{Optional expiry (out-of-force) date range
(\code{Fassung.VonAusserkrafttretensdatum} /
\code{Fassung.BisAusserkrafttretensdatum}, \code{YYYY-MM-DD}).}

\item{section_from, section_to}{Optional section (article/paragraph/annex)
range to narrow within a norm (\code{Abschnitt.Von} / \code{Abschnitt.Bis}).}

\item{section_type}{Optional section type (\code{Abschnitt.Typ}). One of
\code{"Alle"}, \code{"Artikel"}, \code{"Paragraph"}, \code{"Anlage"} (English aliases
\code{"all"}, \code{"article"}, \code{"paragraph"}, \code{"annex"}). Required by the API when
a section range is given; defaults to \code{"Alle"} in that case.}

\item{in_ris_since}{Optional RIS recency filter (\code{ImRisSeit}). Accepts API
values (\code{"Undefined"}, \code{"EinerWoche"}, \code{"ZweiWochen"}, \code{"EinemMonat"},
\code{"DreiMonaten"}, \code{"SechsMonaten"}, \code{"EinemJahr"}) and English aliases
(\code{"one_week"}, \code{"two_weeks"}, \code{"one_month"}, \code{"three_months"},
\code{"six_months"}, \code{"one_year"}).}

\item{sort_by}{Optional sort column (\code{Sortierung.SortedByColumn}). One of
\code{"ArtikelParagraphAnlage"}, \code{"Kurzinformation"}, \code{"Inkrafttretensdatum"},
\code{"Ausserkrafttretensdatum"}.}

\item{sort_direction}{Optional sort direction (\code{Sortierung.SortDirection}):
\code{"Ascending"} or \code{"Descending"}.}

\item{base_url}{API base URL. Defaults to \code{\link[=ris_base_url]{ris_base_url()}}, which can be
overridden for a session via \code{options(risAT.base_url = ...)}.}
}
\value{
An \code{httr2_request} object with an additional \code{"ris_meta"} attribute
containing the application code, page size, endpoint, and website URLs.
Pass this to \code{\link[=ris_perform_federal]{ris_perform_federal()}} to execute the search.
}
\description{
Construct an \code{httr2_request} object for the Austrian RIS OGD REST API v2.6
\verb{/Bundesrecht} endpoint, application \strong{BrKons} (consolidated federal law).
The request is \strong{not executed}; call \code{\link[=ris_perform_federal]{ris_perform_federal()}} to send it
and parse the results, or use \code{\link[httr2:req_dry_run]{httr2::req_dry_run()}} to inspect the URL.
}
\examples{
\dontrun{
# Build request, then inspect the URL without hitting the network
req <- ris_req_federal(title = "ABGB")
httr2::req_dry_run(req)

# The consolidated text as it stood on a given date
req <- ris_req_federal(title = "MRG", version_date = "2020-01-01")

# Execute
results <- ris_perform_federal(req)
}
}
