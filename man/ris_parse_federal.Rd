% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ris_parse_federal.R
\name{ris_parse_federal}
\alias{ris_parse_federal}
\title{Parse RIS Bundesrecht Search Responses}
\usage{
ris_parse_federal(x, requested_page = NULL, requested_per_page = NULL)
}
\arguments{
\item{x}{An \code{httr2_response} or a decoded list.}

\item{requested_page}{Optional requested page number for metadata fallback.}

\item{requested_per_page}{Optional requested page size for metadata fallback.}
}
\value{
A tibble parsed from one RIS Bundesrecht response payload.
Includes list-columns \code{content_urls} and \code{app_metadata}.
}
\description{
Parse a RIS OGD REST API \verb{/Bundesrecht} (BrKons, consolidated federal law)
search response into a tidy tibble. Works with either an \code{httr2_response}
object or an already decoded response list.
}
\examples{
payload <- list(
  OgdSearchResult = list(
    OgdDocumentResults = list(
      Hits = list(pageNumber = "1", pageSize = "20", `#text` = "1"),
      OgdDocumentReference = list(
        list(
          Data = list(
            Metadaten = list(
              Technisch = list(ID = "NOR-1", Applikation = "BrKons"),
              Bundesrecht = list(
                Titel = "Allgemeines bürgerliches Gesetzbuch",
                Kurztitel = "ABGB"
              )
            ),
            Dokumentliste = list(
              ContentReference = list(
                list(
                  Urls = list(
                    ContentUrl = list(
                      list(DataType = "Html", Url = "https://example.org/1.html")
                    )
                  )
                )
              )
            )
          )
        )
      )
    )
  )
)
ris_parse_federal(payload)
}
